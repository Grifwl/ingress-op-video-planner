"""Ingress Operation Video Planner.

Converts an Ingress fielding-operation plan (portal list and ordered link
list) into three timestamped CSV layers ready to import into QGIS with the
Temporal Controller, so the operation can be rendered as an animated video.
"""

__version__ = "0.1.0"
